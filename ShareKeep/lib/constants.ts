// アプリ全体で共有するマジックナンバーの集約先（純粋リファクタリング。値は不変）。

// XP からステージ（0〜4）を算出する際のしきい値。
// xp が各しきい値「未満」なら対応する index のステージ、いずれも満たさなければ最終ステージ。
// 例: xp < 100 → 0, xp < 300 → 1, xp < 600 → 2, xp < 1000 → 3, それ以外 → 4。
export const XP_LEVEL_THRESHOLDS = [100, 300, 600, 1000] as const;

// 地図初期表示などで使うフォールバック座標（東京駅周辺）。
// 代理人が居ない／緯度経度が無い時に使う。
export const FALLBACK_LOCATION = {
  latitude: 35.681236,
  longitude: 139.767125,
} as const;

// 候補探索の半径。実機 GPS 誤差で「見つからない」事故を避けるため広めに取る（既存の自動マッチと同値）。
export const SEARCH_RADIUS_M = 5000;
