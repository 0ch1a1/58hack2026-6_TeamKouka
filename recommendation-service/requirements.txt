# pydantic は v2 前提（schemas.py が `X | None` / model_config を使用）。
# v1 に解決されると起動失敗するため下限を固定する。
fastapi>=0.110
uvicorn>=0.29
scikit-learn>=1.3
pandas>=2.0
numpy>=1.24
joblib>=1.3
supabase>=2.0
# supabase の推移的依存だが、verify_user_token で直接 import するため明示する。
httpx>=0.24
pydantic>=2.5
python-dotenv>=1.0
